openapi: 3.0.0
info:
  title: GoSpeedTest API
  description: High-performance page speed analysis engine written in Go.
  version: 1.0.0
servers:
  - url: http://localhost:8080
    description: Local development server

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key

  schemas:
    Job:
      type: object
      properties:
        id:
          type: string
        url:
          type: string
        status:
          type: string
          enum: [PENDING, RUNNING, COMPLETED, PARTIAL, FAILED]
        tiers:
          type: array
          items:
            type: string
        runs:
          type: integer
        webhook_url:
          type: string
        error:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        budget:
          $ref: '#/components/schemas/Budget'
        budget_result:
          $ref: '#/components/schemas/BudgetEvaluation'

    Budget:
      type: object
      description: >
        Performance budget: assertions keyed by metric name
        (e.g. network.ttfb_ms, network.total_ms, network.dns_lookup_ms,
        network.tls_handshake_ms, network.response_bytes, browser.page_load_ms,
        browser.dom_content_loaded_ms, browser.resource_count, vitals.lcp_ms,
        vitals.fcp_ms, lighthouse.performance, lighthouse.accessibility,
        lighthouse.best_practices, lighthouse.seo).
      required:
        - assertions
      properties:
        assertions:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/BudgetAssertion'
      example:
        assertions:
          network.ttfb_ms: { max: 500 }
          vitals.lcp_ms: { max: 2500, level: warn }
          lighthouse.performance: { min: 0.9 }

    BudgetAssertion:
      type: object
      description: Threshold on one metric. At least one of max/min is required.
      properties:
        max:
          type: number
        min:
          type: number
        level:
          type: string
          enum: [warn, error]
          default: error

    BudgetEvaluation:
      type: object
      description: >
        Outcome of evaluating the budget against the median of the job's runs.
        Evaluated at job completion; never affects the job status itself.
      properties:
        status:
          type: string
          enum: [pass, warn, fail]
        assertions:
          type: array
          items:
            type: object
            properties:
              metric: { type: string }
              level: { type: string, enum: [warn, error] }
              op: { type: string, enum: [max, min] }
              threshold: { type: number }
              actual:
                type: number
                nullable: true
                description: null when the metric was never collected (that trips the assertion).
              status: { type: string, enum: [pass, warn, fail] }

    MetricStats:
      type: object
      properties:
        count: { type: integer }
        avg: { type: number }
        p50: { type: number }
        p75: { type: number }
        p95: { type: number }

    History:
      type: object
      properties:
        url: { type: string }
        test_count: { type: integer }
        avg_ttfb_ms:
          type: number
          description: Legacy field, equals metrics["network.ttfb_ms"].avg.
        avg_total_ms:
          type: number
          description: Legacy field, equals metrics["network.total_ms"].avg.
        metrics:
          type: object
          description: Keyed by budget metric names (e.g. network.ttfb_ms, vitals.lcp_ms).
          additionalProperties:
            $ref: '#/components/schemas/MetricStats'

    CreateJobRequest:
      type: object
      required:
        - url
      properties:
        url:
          type: string
          example: https://example.com
        tiers:
          type: array
          items:
            type: string
            enum: [network, browser, vitals, lighthouse, all]
        runs:
          type: integer
          default: 1
          maximum: 10
        timeout_s:
          type: integer
          description: Per-run timeout in seconds. 0 uses the server default (GOST_TIMEOUT_S).
          minimum: 0
          maximum: 600
        webhook_url:
          type: string
        budget:
          $ref: '#/components/schemas/Budget'

    Result:
      type: object
      properties:
        run_index:
          type: integer
        network:
          type: object
        browser:
          type: object
        vitals:
          type: object
        collected_at:
          type: string
          format: date-time

paths:
  /v1/health:
    get:
      summary: Liveness check
      responses:
        '200':
          description: Server is alive
          content:
            text/plain:
              schema:
                type: string
                example: OK

  /v1/ready:
    get:
      summary: Readiness check
      responses:
        '200':
          description: Server is ready (plain-text body "READY")
          content:
            text/plain:
              schema:
                type: string
                example: READY
        '503':
          description: Database not ready

  /v1/jobs:
    post:
      summary: Submit a new test job
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateJobRequest'
      responses:
        '202':
          description: Job accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  job_id: { type: string }
                  status: { type: string }
        '400':
          description: Invalid request (bad URL, invalid webhook_url, invalid budget, or runs > 10)
        '401':
          description: Unauthorized

    get:
      summary: List recent jobs
      security:
        - ApiKeyAuth: []
      responses:
        '200':
          description: A list of jobs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Job'

  /v1/jobs/{id}:
    get:
      summary: Get job status and results
      security:
        - ApiKeyAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Job details and result payload
        '404':
          description: Job not found

    delete:
      summary: Delete a job (cancels it first if still pending)
      security:
        - ApiKeyAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '204':
          description: Job deleted (its results and webhook deliveries are removed too)

  /v1/history:
    get:
      summary: Get performance aggregations for a URL
      security:
        - ApiKeyAuth: []
      parameters:
        - name: url
          in: query
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Historical statistics (avg/p50/p75/p95 per metric)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/History'
